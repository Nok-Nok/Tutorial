const app = document.getElementById("app");
if (app) {
  app.innerHTML = `
  <h1>Union Types</h1>
  <p>We'll practice Union types by writing a few. Check out the prompts in the <code>/src/unionTypes/ts</code> file.
  `;
}
